#include <stdio.h>
#include "Student.h"

void getList(Student aStu[], int number);
int save(Student aStu[], int number);

int main(int argc, char const *argv[]) {
  int number = 0;
  printf("the number of the students you want to input: ");
  scanf("%d", &number);
  Student aStu[number];

  getList(aStu, number);
  if 
  return 0;
}
